// import {RECEIVE_ONE_KANJI, RECEIVE_ALL_KANJIS } from './../actions/user_actions


const kanjiReducer = (state = {}, action) => {
    Object.freeze(state);
    


    switch(action.type) {
        default:
            return state;
    }
}

export default kanjiReducer